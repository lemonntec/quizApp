// ignore_for_file: prefer_typing_uninitialized_variables, unused_import

import 'package:flutter/material.dart';

class myQuestion {
  var questions;
  bool isCorrect;

  myQuestion.name(this.questions, this.isCorrect);
}
